import React, { Fragment } from "react";
import NavBar from "../../components/NavBar";

const NavBarPage = () => {
  return (
    <Fragment>
      <NavBar />
    </Fragment>
  );
};

export default NavBarPage;
